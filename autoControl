from motor_control import AlphaBot2
class control :
    slope = 0

    def autoDriving(slope) :

        if(slope > 86  and slope < 96):
            Ab.forward()
            print("Forward")

        elif vp_slope > 96 :
            print("Turn Right")
            Ab.right()
            time.sleep(0.5) 
        
        elif vp_slope < 86 :
            print("Turn LEFT")
            Ab.left()
            time.sleep(0.5)

        else:
            print("STOP")
            Ab.stop()
